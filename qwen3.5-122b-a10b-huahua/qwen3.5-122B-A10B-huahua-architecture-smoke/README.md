# Architecture Smoke

This bundle retains the first 122B single-prompt architecture smoke run and its token audit.

## Status
- Compact artifacts retained locally.
- This bundle exists as historical follow-up context only.
- It is not part of the canonical baseline root.

## Artifacts
- Prompt source: `PROMPTS/architecture_probe_single_prompt.json`
- Run metadata: `RESULTS/*architecture_probe_single_prompt*`
- Compact raw artifacts: `raw/`
